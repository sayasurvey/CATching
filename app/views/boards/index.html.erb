<% content_for(:title, "動画一覧 | CATching") %>

<div class='content m-5'>
  <h1 class='title p-4'>動画一覧</h1>

  <!-- 掲示板一覧 -->
  <div class='row'>
    <div class='col-12 my-5'>
      <div class='row'>
        <% if @boards.present? %>
          <div class='card-deck col-12'>
            <%= render @boards %>
          </div>
        <% else %>
          <p>該当記事がありません</p>
        <% end %>
      </div>
    </div>
  </div>
</div>
